-
	const timestamp = Date.now();
doctype html
html
	head
		meta(charset="utf-8")
		title Trabi II Remake
		meta(name="viewport" content="width=device-width, user-scalable=no")
		link(rel="stylesheet" type="text/css" href=`main.min.css?v${timestamp}`)
		
		script(src=`phaser-arcade-physics.js?v${timestamp}`)
		//- script(src=`https://cdn.jsdelivr.net/npm/phaser@3.90.0/dist/phaser-arcade-physics.js`)
		//- script(src=`https://cdn.jsdelivr.net/npm/phaser@3.90.0/dist/phaser-arcade-physics.min.js`)
	body
		script(src=`main.min.js?v${timestamp}`)
