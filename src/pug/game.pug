-
	const timestamp = Date.now();
doctype html
html
	head
		meta(charset="utf-8")
		title Trabi II Remake
		meta(name="viewport" content="width=device-width, user-scalable=no")
		link(rel="stylesheet" type="text/css" href=`main.min.css?v${timestamp}`)
		
		script(src=`phaser.js?v${timestamp}`)
		//- script(src=`https://cdn.jsdelivr.net/npm/phaser@3.80.1/dist/phaser.js`)
		//- script(src=`https://cdn.jsdelivr.net/npm/phaser@3.80.1/dist/phaser.min.js`)
	body
		script(src=`main.min.js?v${timestamp}`)
